# Secrets / environment — never commit real values
.env
frontend/.env

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Frontend dependencies + build output (hundreds of MB, reproducible via npm)
frontend/node_modules/
frontend/dist/

# OS / editor noise
.DS_Store
*.log
nohup.out
test_output.json

